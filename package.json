{
  "name": "keskinlab",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-site-data.mjs",
    "optimize:sources": "node scripts/optimize-source-images.mjs",
    "build:web-assets": "node scripts/build-web-assets.mjs",
    "validate:html": "node scripts/validate-html-links.mjs",
    "validate:site": "node scripts/validate-site.mjs",
    "validate:courses": "node scripts/validate-course-data.mjs",
    "validate:navigation": "node scripts/validate-navigation.mjs",
    "validate:a11y": "node scripts/validate-accessibility.mjs",
    "validate:mirrors": "node scripts/validate-course-mirrors.mjs",
    "validate": "npm run validate:html && npm run validate:site && npm run validate:courses && npm run validate:navigation && npm run validate:a11y && npm run validate:mirrors",
    "validate:strict-assets": "node scripts/validate-site.mjs --strict-assets && npm run validate:courses",
    "check": "npm run generate && npm run validate",
    "build": "npm run build:web-assets && npm run generate && npm run validate:html && npm run validate:strict-assets && npm run validate:navigation && npm run validate:a11y && npm run validate:mirrors"
  },
  "dependencies": {
    "sharp": "^0.35.4"
  }
}
